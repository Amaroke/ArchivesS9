export type Reservation = {
  numero: string; // Le numéro de la réservation
  immatriculation: string; // L'immatriculation du véhicule
  numeroMembre: number; // Le numéro du membre
  datePret: string; // La date de prêt
  etat: string; // L'état de la réservation
};
